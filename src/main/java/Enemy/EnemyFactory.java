package Enemy;

public class EnemyFactory {

    public static Enemy createEnemy(String type) {
        if(type == null) {
            return null;
        }
        switch(type.toUpperCase()) {
            case
        }
    }
}
